/* query is to tell dbs to give exactly what piece of information you wanna collect from dbs
DECIMAL (10,4) MEANS TOTAL A NUMBER WILL CONSIST OF 10 DIGIT IN WHICH 4 WILL BE AFTER THE DECIMAL POINT(.) AND 6 BEFORE IT.
INT --> FOR WHOLE NO.
VARCHAR(20) --> FOR STRING CONTAINING AT MOST 20 CHAR VARIABLES
DATE --> FOR "DD-MM-YY"  FORMAT
TIMESTAMP --> FOR "DD-MM-YY" AND "HH:MM:SS" FORMAT */
CREATE TABLE STUDENT(
STUDENT_ID INT PRIMARY KEY,
NAME VARCHAR(20),
PROFFF VARCHAR(20)
);
/* or can be also be written as */
CREATE TABLE STUDENT(
STUDENT_ID INT,
NAME VARCHAR(20),
PROFFF VARCHAR(20),
PRIMARY KEY(STUDENT_ID)
);
DESCRIBE STUDENT;
DROP TABLE STUDENT;
ALTER TABLE STUDENT ADD SGPA DECIMAL(2,2); /*TO ADD COLUMS OR ATTRIBUTE*/
ALTER TABLE STUDENT DROP COLUMN SGPA; /*TO DROP ATTRIBUTE*/
NOW TO INSERT DATA 
CREATE TABLE STUDENT(
STUDENT_ID INT PRIMARY KEY,
NAME VARCHAR(20),
PROFFF VARCHAR(20)
);
SELECT * FROM STUDENT;
INSERT INTO STUDENT VALUE(1,'ABHI','THINK TANK');
INSERT INTO STUDENT(STUDENT_ID,NAME) VALUE(2,'JAISHANKAR'); --ANOTHER WAY TO INSERT DATA
